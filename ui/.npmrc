# pnpm 11 enabled a supply-chain policy by default that rejects packages
# published within the last 7 days. We do regular dependency bumps and don't
# want fresh upstream releases to break our builds — disable the age check.
# If you want to dial it back up for security, set a value in seconds.
minimum-release-age=0
